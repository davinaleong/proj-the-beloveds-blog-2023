---
import { marked } from 'marked'

const { id = "next", sectionClassName, containerClassName, page } = Astro.props
const { text } = page
const content = marked.parse(text)
---
<section id={id} class={"section " + sectionClassName}>
    <div class={"container " + containerClassName} set:html={content} />
</section>
