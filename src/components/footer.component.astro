---
import LinkAstroComponent from "./../components/link.component.astro"

const { title, metaAuthor, releasedAt } = Astro.props

let thisReleasedAt = import.meta.env.APP_RELEASED_AT
if (releasedAt && releasedAt !== "") {
    thisReleasedAt = releasedAt
}

const releasedYear = Number(new Date(`${thisReleasedAt}T00:00:00Z`).getFullYear())
const nowYear = Number(new Date().getFullYear())

const copyrightYear = nowYear !== releasedYear ? `${releasedYear} - ${nowYear}` : releasedYear
---
<footer class="primary-footer">
    <div class="container primary-footer-container | ta-center fz-copyright">
        <p class="m-v-b-200">{title} &copy; {metaAuthor}, {copyrightYear}.</p>
        <p>
            View the <LinkAstroComponent href="/privacy" className="btn-link">Privacy Policy</LinkAstroComponent> to understand how The Beloved's Blog consumes your data.
        </p>
    </div>
</footer>